@using Microsoft.AspNet.Identity

<nav id="navbar" class="navbar">
    <ul>

        <li>
            @Html.ActionLink("Inicio", "Index", "Home")
        </li>
        @{
            if (Request.IsAuthenticated)
            {
                <li>
                    @Html.ActionLink("Clientes", "Index", "Clientes")
                </li>
                <li>
                    @Html.ActionLink("Productos", "Index", "Productos")
                </li>
                <li class="dropdown">
                    <a href="#"><span>Admin</span> <i class="bi bi-chevron-down"></i></a>
                    <ul>
                        <li>
                            @Html.ActionLink("Tipos de Transaccion", "Index", "TipoTransaccions")
                        </li>
                        <li>
                            @Html.ActionLink("Roles", "Index", "Rols")
                        </li>
                    </ul>
                </li>
            }
        }
        @Html.Partial("_LoginPartial")
    </ul>
    <i class="bi bi-list mobile-nav-toggle"></i>
</nav>
